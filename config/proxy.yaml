# Generic NocoDB Proxy Configuration
# This file defines the schema-driven configuration for the proxy
nocodb:
  base_id: "ptteoble0x5644w"  # Your base ID

tables:
  customers:
    name: "Customers"  # Must match exactly as in NocoDB
    id: "mti3vlhc9gbtbu0"  # Explicitly set the table ID
    operations: [read, create, update, delete]

  devices:
    name: "Devices"  # Must match exactly as in NocoDB
    id: "mapitmsn1xplcps"  # Explicitly set the table ID
    operations: [read, create, update, delete]

  amc:
    name: "AMC"  # Must match exactly as in NocoDB
    id: "msobv13v7s1h4nw"  # Explicitly set the table ID
    operations: [read, create, update, delete]


  credentials:
    name: "Credentials"  # Must match exactly as in NocoDB
    id: "mvg180heeisqpfg"  # Explicitly set the table ID
    operations: [read, create, update, delete]
  # products:
  #   name: "Products"
  #   operations: [read]

  # accounts:
  #   name: "Accounts"
  #   operations: [read]

  # contacts:
  #   name: "Contacts"
  #   operations: [read]

  # accounts_quotes:
  #   name: "Accounts Quotes"
  #   operations: [read, create, link]

  # products_for_quotes:
  #   name: "ProductsForQuotes"
  #   operations: [read, create, link]
