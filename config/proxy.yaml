# Generic NocoDB Proxy Configuration
# This file defines the schema-driven configuration for the proxy
nocodb:
  base_id: "pyply12zyphcmmm"  # Your base ID

tables:
  team_members:
    name: "Team Members"  # Must match exactly as in NocoDB
    id: "mrumjq78h7hs1bn"  # Explicitly set the table ID
    operations: [read, create, update, delete, link]

  objectives:
    name: "Objectives"  # Must match exactly as in NocoDB
    id: "ml2x0cn01o484zu"  # Explicitly set the table ID
    operations: [read, create, update, delete, link]

  tasks:
    name: "Tasks"  # Must match exactly as in NocoDB
    id: "mv3bf1o9hbh78ey"  # Explicitly set the table ID
    operations: [read, create, update, delete, link]
  # products:
  #   name: "Products"
  #   operations: [read]

  # accounts:
  #   name: "Accounts"
  #   operations: [read]

  # contacts:
  #   name: "Contacts"
  #   operations: [read]

  # accounts_quotes:
  #   name: "Accounts Quotes"
  #   operations: [read, create, link]

  # products_for_quotes:
  #   name: "ProductsForQuotes"
  #   operations: [read, create, link]
